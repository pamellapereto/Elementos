import { View, Text } from 'react-native';
import { format } from '../../css/Styles';

export default function Footer() {
    return(
        <View style={format.ft}>
            <Text style={format.txtFt}>Footer</Text>
        </View>
    )
w
}